<resources>

    <!-- 自定义BottomPushPopupWindow动画，从底部弹出 -->
    <style name="BottomPushPopupWindow" parent="android:Animation">
        <!-- 进入时的动画 -->
        <item name="android:windowEnterAnimation">@anim/pw_push_bottom_in</item>
        <!-- 退出时的动画 -->
        <item name="android:windowExitAnimation">@anim/pw_push_bottom_out</item>
    </style>

    <!-- 自定义TopPushPopupWindow动画，从顶部弹出 -->
    <style name="TopPushPopupWindow" parent="android:Animation">
        <!-- 进入时的动画 -->
        <item name="android:windowEnterAnimation">@anim/pw_push_top_in</item>
        <!-- 退出时的动画 -->
        <item name="android:windowExitAnimation">@anim/pw_push_top_out</item>
    </style>


    <!-- 自定义proDialog -->
    <style name="proDialog" parent="@android:style/Theme.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <!--背景透明-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--无主题-->
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- 自定义diydialog样式 -->
    <style name="diydialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@drawable/bg_white_gray_border</item>
        <!-- 背景 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 是否为半透明 -->
    </style>

</resources>
